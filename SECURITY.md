# 보안 정책 (Security Policy)

## 🔐 API 키 보호

이 프로젝트는 여러 외부 AI 서비스 API 키를 사용합니다.
**키 노출은 즉시 금전적 손실로 이어질 수 있습니다.**

### 안전 수칙

1. **`.env` 파일은 절대 커밋하지 마세요**
   - `.gitignore`에 포함되어 있지만, 강제 추가하지 마세요
2. **키를 코드에 하드코딩하지 마세요**
   - 항상 `os.getenv()` 또는 `python-dotenv` 사용
3. **로그·에러 메시지에 키를 출력하지 마세요**
   - `src/utils/env.py`의 `mask_key()` 사용 권장
4. **스크린샷·녹화에 키 노출 주의**
   - 시연·강의 시 `.env`를 더미 키로 교체
5. **공유 PC·노트북 사용 후 정리**
   - 사용 종료 시 `.env`를 외부 USB로 백업 후 삭제 권장

## 🚨 만약 키가 유출되었다면

**즉시 다음을 실행하세요:**

1. **해당 키 폐기**
   - Anthropic: https://console.anthropic.com → API Keys → Revoke
   - ElevenLabs: 프로필 → API Keys → Delete
   - OpenAI: https://platform.openai.com → API keys → Revoke
   - Higgsfield: 대시보드에서 토큰 재발급

2. **새 키 발급 후 `.env` 갱신**

3. **Git 히스토리에 남은 경우**
   ```bash
   git rm --cached .env
   git commit -m "Remove leaked .env"
   git push --force
   ```
   > 협업 중이라면 동료에게 미리 알리고, 그 키는 **반드시 폐기**하세요.
   > Git 히스토리에서 완전히 지우는 건 매우 어렵습니다.

4. **결제 내역 확인**
   - 비정상 사용량이 있다면 각 서비스 고객지원에 즉시 문의

## 🛡 취약점 신고

이 프로젝트에서 보안 취약점을 발견했다면:

- **공개 Issue 사용 금지** (악용 가능성 때문)
- 메인테이너에게 비공개 연락:
  - GitHub: @YOUR_USERNAME (Security Advisory 기능 사용)
  - 이메일: (프로젝트 운영자가 추가)

48시간 이내에 회신하며, 검증 후 패치합니다.

## 🎓 AI 생성 콘텐츠 안전

이 프로젝트는 AI로 영상을 생성합니다. 다음 위험을 인지하세요.

### 콘텐츠 위험
- **사실 오류**: AI가 부정확한 정보를 생성할 수 있음
- **편향**: 특정 관점·문화에 치우친 결과 가능
- **부적절한 묘사**: 학생에게 부적합한 이미지·표현

→ **모든 출력물은 사용 전 사람이 검수**해야 합니다.

### 저작권·초상권
- 실존 인물·캐릭터를 생성하지 마세요
- 브랜드 로고·트레이드드레스 무단 생성 금지
- 학생 사진을 학습 데이터로 업로드하지 마세요

### 미성년자 보호
- 미성년자를 사실적으로 묘사하는 이미지 생성 금지
- 학생 개인정보를 프롬프트에 포함하지 마세요
- 학생에게 보여주기 전 콘텐츠를 항상 검토

## 📜 지원되는 버전

| 버전 | 보안 패치 |
|------|----------|
| 1.x | ✅ 적용 |
| 1.x 이전 | ❌ 미지원 |

---

이 정책은 사용자, 학생, 기여자 모두를 보호하기 위해 존재합니다.
함께 안전한 환경을 만들어주세요.
